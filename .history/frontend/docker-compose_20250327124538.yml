version: '3.8'
services:
  frontend:
    image: node:18
    working_dir: /frontend
    volumes:
      - .:/frontend
    ports:
      - "8911:8911"
      - "3000:3000"

    command: ["npm", "run", "dev"]  
    
    restart: unless-stopped



