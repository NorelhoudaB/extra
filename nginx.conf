events {}
http {
  server {
    listen 80;
    server_name localhost;

    
    location / {
      proxy_pass http://frontend:8911;
      proxy_set_header Host $host;
    }

    location /api {
      proxy_pass http://backend:8888;
      proxy_set_header Host $host;
    }
  }
}